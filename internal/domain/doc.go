// Package domain holds the core entities, the channel-type configuration
// model, the permission engine and channel identity rules (SPEC.md §4, §6,
// §7). It has no dependencies on transport or persistence packages.
package domain
